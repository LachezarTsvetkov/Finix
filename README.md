# Finix

Steps to run:
- Clone the repository using `git clone https://github.com/LachezarTsvetkov/Finix.git`
- Update the sumbodules by running `git submodule init` and `git submodule update`
- Run the 'Generateprojects.bat' to setup the solutions
- Build the Finix engine
- Build and run the Sandbox